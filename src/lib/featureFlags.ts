/**
 * Banderas de funcionalidades que se pueden apagar sin borrar código ni datos.
 * Reservas automáticas activas: al crear un pedido las unidades cubiertas se apartan
 * (disponible -> reservado) y se liberan al cancelar el pedido o manualmente
 * desde Inventarios/Administrador.
 */
export const RESERVATIONS_ENABLED = true;

