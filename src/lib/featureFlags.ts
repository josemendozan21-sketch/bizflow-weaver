/**
 * Banderas de funcionalidades que se pueden apagar sin borrar código ni datos.
 * El sistema de reservas queda oculto en el frontend hasta nueva orden:
 * la tabla, los triggers y los movimientos históricos siguen intactos.
 */
export const RESERVATIONS_ENABLED = false;
